require('dotenv').config();const mongoose=require('mongoose');const Template=require('../models/Template');(async()=>{try{await mongoose.connect(process.env.MONGO_URI||process.env.MONGODB_URI);console.log(await Template.updateMany({accessType:{$exists:false}},{$set:{accessType:'free',requiredPlan:'free',featuredOnHome:false,isActive:true,displayOrder:0}}));}finally{await mongoose.disconnect();}})().catch(e=>{console.error(e);process.exit(1);});
